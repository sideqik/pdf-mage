#!/usr/bin/env ruby
begin
  require 'pdf_mage'
rescue LoadError => e
  require 'rubygems'
  path = File.expand_path '../../lib', __FILE__
  $:.unshift(path) if File.directory?(path) && !$:.include?(path)
  require 'pdf_mage'
end

PdfMage.start!
